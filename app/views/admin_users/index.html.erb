<br>

<% for user in @users %>
    <fieldset>
        <%= user.email%>
        <% if user.admin?%>
            <%= "User Type: admin"%>
        <% else %>
            <%= "User Type: user"%>
            <% if user.ban?%>
                <%= "User Permission Status: Banlı"%>
            <% end %>
        <% end%>
        <%= link_to "Show Profile", admin_user_path(user.id)%>
        <br>
    </fieldset>
<% end %>

